'use client';

import React, { useEffect, useState } from "react";
import { useSupabaseClient, useSession } from "@supabase/auth-helpers-react";

// Show all stations that are marked as "Desert" with the similar styling (for now) as the regular `EarthViewLayout`

export default function DesertBaseEarthScene() {
    return (
        <></>
    );
};