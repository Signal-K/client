import React from "react";
import Link from "next/link";

/*const DisplayProposals = ({ title, isLoading, proposals }) => {
    return (
        <div>
            <h1 className="font-epilogue font-semibold text-[18px] text-white text-left">{title} ({proposals.length}) </h1>
            <div className="flex flex-wrap mt-[20px] gap-[26px]">
                {proposals.length === 0 && (
                    <p className="font-epilogue font-semibold text-[14px] leading-[30px] text-[#818183]">You have not created any proposals yet</p>
                )}
                {proposals.length > 0 && proposals.map((proposal) => <FundCard
                    key={proposal.id}
                    {...proposal}
                    handleClick={() => <Link></Link>})}
            </div>
        </div>
    )
}*/