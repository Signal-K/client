import { Badge } from "@/components/ui/badge"
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { ArrowLeft, CheckCircle2, Clock, Eye, Mountain, Wind, Gem, Map, Cloud, Truck } from "lucide-react"
import type { SurfaceAnomaly, RoverMission, RoverProject } from "@/types/Structures/Rover"
import { roverProjects } from "@/data/projects"
import { rovers } from "@/data/roverList"

export default function RoverDiscoveries() {
    return (
        <></>
    );
};